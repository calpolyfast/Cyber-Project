name: Docker Image Cli

on:
  push:
    branches: [ "*" ]
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Debug file structure
        run: ls -R

      # -------- Backend Image --------
      - name: Build and push backend image
        uses: docker/build-push-action@v5
        with:
          context: ./app/backend
          file: ./app/backend/Dockerfile
          push: true
          tags: raglem/backend-app:latest

      # -------- Playwright image --------
      - name: Build and push playwright image
        uses: docker/build-push-action@v5
        with:
          context: ./launcher/api/playwright
          file: ./launcher/api/playwright/Dockerfile
          push: true
          tags: raglem/playwright-browser:latest

